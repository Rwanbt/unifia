[package]
name = "unifia-voice-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Platform-independent Voice event and error contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
