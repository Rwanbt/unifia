<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Legacy storage perms (Android 10 and below; silently ignored on SDK 33+) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <!-- Android 13+ scoped media perms: makes the "Photos and videos" / "Music and audio"
         entries appear under Settings → Apps → OpenCode → Permissions. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Full filesystem access for the embedded terminal (git/editors in arbitrary dirs).
         Shown in the separate "All files access" screen, not the standard Permissions list. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Foreground Service to host llama-server child processes.
         Without this, Android 12+ kills spawned llama-server as a phantom process
         ~20s after startup, cascading the kill to the parent Activity. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Prevent CPU deep sleep during LLM inference. Without this, the SoC can
         enter idle power states between token generation steps when the screen is
         off, causing intermittent ~200ms stalls and degraded throughput.
         The lock is acquired in LlamaService.spawnServer() and released in
         stopChildProcess(). A ForegroundService alone does NOT imply a wake lock. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- A held WAKE_LOCK keeps the CPU from deep-sleeping but does NOT exempt the
         app from Doze's network restrictions — confirmed on-device 2026-07-02:
         `dumpsys deviceidle whitelist` did not list this app, and the bun sidecar
         (unifia-cli.js, port 14096) stayed unreachable for the full Dozing
         window even with the WAKE_LOCK above actively held. The sidecar is the
         entry point for every request (including ones that only need llama-server),
         so this exemption is requested at startup — see MainActivity.requestBatteryOptimizationExemption(). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Speech-to-text: WebView captures mic via getUserMedia; RustWebChromeClient
         forwards the AUDIO_CAPTURE request to the Android permission dialog. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:name=".OpenCodeApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.unifia_mobile"
        android:extractNativeLibs="true"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <!-- OpenCL for GPU-accelerated local LLM inference -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Custom scheme for QR-based pairing from desktop Settings → Remote Access -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="opencode" />
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
            <intent-filter >
                <action android:name="android.intent.action.VIEW" />
                <!-- ChromeOS ARC++ uses a different action for deep links -->
                <action android:name="org.chromium.arc.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="unifia" />
                
                
                
                
                
            </intent-filter>
            <!-- DEEP LINK PLUGIN. AUTO-GENERATED. DO NOT REMOVE. -->
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>

        <!-- Foreground Service hosting llama-server child processes.
             foregroundServiceType="specialUse" is required because LLM inference
             does not fit any predefined type (mediaPlayback, dataSync, etc.).
             The PROPERTY_SPECIAL_USE_FGS_SUBTYPE is mandatory since API 34. -->
        <service
            android:name=".LlamaService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-device LLM inference host for llama-server child process" />
        </service>
    </application>
</manifest>
