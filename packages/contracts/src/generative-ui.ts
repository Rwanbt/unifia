/* SPDX-License-Identifier: MIT */
export type UiNode = { type: "text" | "button" | "input" | "panel"; id: string; props?: Record<string, string>; children?: readonly UiNode[] }
export type RenderedUiNode = { type: UiNode["type"]; id: string; props: Readonly<Record<string, string>>; children: readonly RenderedUiNode[] }
const ALLOWED_PROPS: Readonly<Record<UiNode["type"], readonly string[]>> = { text: ["value"], button: ["label", "actionId"], input: ["label", "value", "actionId"], panel: ["title"] }
export function renderGenerativeUi(node: UiNode, allowedActions: ReadonlySet<string>): RenderedUiNode { if (!/^[A-Za-z0-9_-]+$/.test(node.id)) throw new Error("invalid UI node id"); const allowed = ALLOWED_PROPS[node.type]; if (!allowed) throw new Error("unsupported UI component"); const props: Record<string, string> = {}; for (const key of allowed) { const value = node.props?.[key]; if (value !== undefined) { if (key === "actionId" && !allowedActions.has(value)) throw new Error("UI action is not allowlisted"); props[key] = value } } return { type: node.type, id: node.id, props, children: (node.children ?? []).map((child) => renderGenerativeUi(child, allowedActions)) } }
