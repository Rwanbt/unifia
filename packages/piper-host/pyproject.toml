[project]
name = "unifia-piper-host"
version = "0.1.0"
description = "Isolated CPU-first Piper TTS worker for Unifia"
requires-python = ">=3.12,<3.13"
license = "GPL-3.0-or-later"
dependencies = ["piper-tts==1.8.0"]

[tool.uv]
python-preference = "only-managed"
