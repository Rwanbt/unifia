<!-- SPDX-License-Identifier: MIT -->
# Unifia V3 — état des phases au 2026-08-04

Reconstruit depuis le plan canonique `Plan-directeur-V3-Unifia-WorkBench-OpenWork-OpenCowork.md`,
le handoff Hermes et une lecture directe du code de `D:\App\OpenCode\unifia-execution-clean`.

## Convention de statut

| Statut | Signification |
|---|---|
| `PASS local` | Preuve reproductible sur cette machine (test, typecheck, gate). Ce n'est **pas** une preuve de production. |
| `PARTIEL` | Une partie du livrable existe et est prouvée ; le reste est nommé explicitement. |
| `NON FAIT` | Aucun livrable, ou seulement un document d'intention. |
| `BLOQUÉ EXTERNE` | Nécessite une autorité, un service ou un matériel hors de cette machine. |

**Règle appliquée** : un statut sans preuve citable est écrit `NON PROUVÉ`, jamais `PASS`.

## Tableau

| Phase | Livrable du plan | Statut | Preuve / raison |
|---|---|---|---|
| −2 | Audit licences et provenance | `PARTIEL` | `LICENSE-AUDIT-*`, `UPSTREAM-SOURCES.lock.json`, `DO-NOT-IMPORT.md` présents. Gate automatisé ajouté ce jour : `supply-chain/forbidden-paths`, `excluded-imports`, `spdx-headers`, `manifest-licenses`, `dependency-provenance` — 5/5 PASS. Revue juridique externe : `BLOQUÉ EXTERNE`. |
| −1 | Audit comparatif tri-repo | `PARTIEL` | Six matrices corrigées (commit `375467a`), preuves path-level dans `M1-PATH-EVIDENCE`. Les affirmations non vérifiées sont normalisées `UNVERIFIED`. |
| 0 | Rebrand, gouvernance, upstream | `PARTIEL` | Rebrand desktop/mobile fait (`bb5827c`). Le hook pre-commit `.husky/pre-commit` applique chemins interdits + `.env` + SPDX. |
| 1 | CI, tests, builds, harness multi-runtime | `PASS local` | `FakeRuntimeAdapter` + `OpenCodeRuntimeAdapter` + `UnifiaRuntimeAdapter`. Gate CI : `.github/workflows/unifia-conformance.yml`. **Conformance suite §13 livrée** : `@unifia/runtime-conformance` 30/30 (3 runtimes × 10 scénarios). Réserve explicite : les deux adaptateurs délégants tournent sur un backend construit depuis `FakeRuntimeAdapter` — la suite prouve la conformité au contrat et la délégation, **pas** le comportement du runtime OpenCode réel. |
| 2 | Contrats Unifia et adaptateurs | `PASS local` | `@unifia/contracts` : 6 ports, suites vitest 32/32 + smokes. Typecheck monorepo 21/21. Négociation de version et compat N−1 : `NON FAIT`. |
| 3 | Security foundation, capabilities, ApprovalBroker | `PASS local` | `PolicyEngineDouble`, `ApprovalBroker` 5/5, `CapabilityRegistry` 6/6, `SecretStore`/`KillSwitchRegistry`. Bijection capability↔effet désormais vérifiée (corrigée ce jour : l'assertion était figée à 14 et périmée). |
| 4 | WorkspaceRuntime, stockage, migrations | `PASS local` | `WorkspaceRuntime` 12/12, `DurableQueue` 4/4, `WorkspaceStorage` 12/12 dont **8 checks de conformance migration ajoutés ce jour**. Défaut de perte de données corrigé (`7ff7dd1`). |
| 5 | Extraction OpenWork (serveur, orchestrateur) | `PASS local` | `WorkbenchServer` 72/72 + **`WorkbenchBootstrap` 39/39 sur HTTP réel** (`5590c9d`) : le serveur est désormais un processus, avec écoute loopback, audit JSONL durable et arrêt propre. Le critère « le serveur fonctionne headless » est atteint localement. **Orchestrateur et MultiWorkspaceRouter livrés** (`@unifia/workbench-orchestrator` 28/28) : le critère de sortie « multi-workspace fonctionne sans relancer inutilement le cœur » est mesuré — 25 changements de workspace, **1 seul runtime construit**. L'isolation passe par le scoping, pas par la multiplication de runtimes ; le routeur refiltre les sessions au cas où le runtime ignorerait le scope. |
| 6 | Documents et artefacts bureautiques | `PASS local` | `DocumentPackRegistry` 6/6, six packs, workers réseau `off`, golden hashes, `inspectStoredZip` anti zip-slip/bombe. |
| **Gate A** | Workbench headless stable | `PASS local, non promu` | Les deux manques cités le 2026-08-04 sont clos : serveur headless réel (`5590c9d`, bootstrap 40/40 sur HTTP) et suite de conformance des adaptateurs (`541238e`, 30/30). Non promu : le backend OpenCode réel n'est pas branché à la suite, et CI distante + audit externe restent hors de cette machine. |
| 7 | Shell Unifia Code/Work | `PARTIEL` | **Les cinq critères de sortie du §20 sont exercés** par `@unifia/workbench-shell` 122/122 : ce sont des affirmations sur l'**adressage et la propriété**, pas sur l'apparence. (a) *Code et Work partagent les sessions* — le même id résout au même objet depuis les deux modes. (b) *Un artefact créé dans Work s'ouvre dans Code/Design* — ouvert depuis les quatre modes, `createdIn` est de la **provenance, jamais une permission**, et survit sans être réécrit. (c) *Le changement de mode ne change pas de runtime* — c'est le critère qui échoue **silencieusement** : un shell où chaque mode construit paresseusement son propre adaptateur est indistinguable de l'extérieur, les sessions **paraissent** partagées jusqu'à ce que deux runtimes divergent. Le shell détient donc **une seule** référence, `switchMode` rend le même objet, et le test **compte les constructions**. (d) *Le shell reste utilisable sans réseau* — tout point d'entrée réseau lève, puis les 4 modes × 11 fonctions, l'aller-retour d'artefact et le chemin de preview sont pilotés. (e) *Le mobile consomme les mêmes contrats en lecture* — la projection est **dérivée** d'une seule liste, pas redéclarée, donc la surface mobile ne peut pas dériver de la définition d'une lecture. Règles UX tenues : provenance obligatoire sur chaque résultat (`surface()` refuse sans), et **preview à usage unique lié à la fois à l'action et à la cible** pour les actions destructives — un preview de suppression de A n'est pas un consentement à supprimer B, et rejouer un jeton n'est pas un consentement donné deux fois ; les actions inoffensives restent hors de ce cérémonial. **NON LIVRÉ, et non revendiqué** : le shell SolidJS rendu. Ce paquet est le modèle qu'il consommerait — la navigation à 4 destinations et les 11 fonctions Work V1 existent ici **en données**, pas en pixels, précisément pour qu'« expérience Work V1 livrée » soit une affirmation contestable. |
| 8 | SandboxBroker multi-backend | `PASS local` (native, wsl2) / `PARTIEL` (docker, lima) | `SandboxBroker` 4/4 et **`@unifia/sandbox-drivers` 29/29** : `NativeRestrictedDriver`, `Wsl2Driver` et `DockerDriver` réels. **WSL2 vérifié en exécution réelle** (conformance 4/4 + commande exécutée). Propriétés prouvées : environnement hôte **non hérité** (une variable piégée n'apparaît pas dans le dump du bac à sable), timeout qui **tue** le processus (exit 124 en <1 s sur un `sleep 20`), politique invalide refusée **à la déclaration** et non à l'exécution, et **aucun repli silencieux** — Docker absent lève `SandboxUnavailableError` au lieu de se rabattre (§35). Lima : `NON FAIT` (pas de macOS ici). |
| 9 | Remote bridges contrôlés | `PASS local` | **Les cinq critères de sortie du §22 sont exercés**, pas affirmés : `@unifia/remote-bridge` 35/35, `RemoteBridgeBroker` 13/13, `SlackRemoteAdapter` 16/16, `FeishuRemoteAdapter` 20/20. L'audit de la ligne précédente était faux sur trois points et les trois sont corrigés. **(1)** « adapters avec signature » : le vérificateur Slack comparait la chaîne `"bolt-verified"` à elle-même — il n'authentifiait rien. Les deux schémas réels sont désormais implémentés (Slack `v0=HMAC-SHA256("v0:{ts}:{body}")`, Feishu `SHA-256("{ts}{nonce}{key}{body}")`), comparés en temps constant, bornés par une fenêtre de dérive — le timestamp est *dans* la chaîne signée, donc la signature seule n'expire jamais. **(2)** Une commande distante omettant `metadata.capability` renvoyait `accepted` : un champ absent suffisait à contourner l'ApprovalBroker (vérifié en exécution avant correction). Le chemin est passé en refus par défaut. **(3)** La route `POST /feishu` en production lisait `c.req.json()` **sans aucune vérification** et relayait la charge dans un canal Discord avec un token de bot — `FeishuRemoteAdapter` n'était importé que par son propre test. La route vérifie maintenant `X-Lark-Signature` et **échoue fermée** sans clé. Appairage désormais acte de l'hôte (dérivé de l'allowlist) et non effet de bord du trafic : un expéditeur inconnu n'alloue plus ni fenêtre de nonce, ni fenêtre de débit, ni entrée d'audit sous un identifiant qu'il choisit. Transports désactivables séparément (`RemoteTransportRegistry`), le kill switch global restant un contrôle distinct qui l'emporte. Révocation mesurée < 1 ms pour un budget d'une seconde. Aucune de ces trois suites ne tournait dans un gate — c'est ainsi que tout cela a survécu ; les trois y sont maintenant. `BLOQUÉ EXTERNE` : la valeur du secret `FEISHU_ENCRYPT_KEY` (déclaré et lié dans `infra/app.ts`) et le déploiement du Worker. |
| 10 | Browser et Computer Use | `PASS local` | `BrowserAutomationBroker`, `DesktopAutomationBroker` 3/3, emergency stop 1/1, et **`@unifia/computer-use-safety` 36/36** couvrant les quatre critères du §23 qui n'avaient rien derrière : **injection visuelle**, **clickjacking / changement de fenêtre**, **perte de focus**, **champs mot de passe**. Conception : une action doit présenter le **reçu d'observation** qui l'a motivée ; tout ce qui est dangereux vit dans l'écart entre regarder et agir, donc un reçu qui ne correspond plus à l'écran est refusé. Observer un écran empoisonné est autorisé **et signalé** — le danger n'est pas de voir le texte, c'est de lui obéir. |
| **Gate B** | Cowork local-first sécurisé | `NON PROUVÉ` | Dépend des drivers sandbox réels et des tests d'injection visuelle. |
| 11 | Spec-driven et OpenDesign | `PARTIEL` | `@unifia/spec-runtime` 35/35 (`43ccdaa`) : schéma de spec strict, ciblage Code/Work/Design/Automate, **invariant du plan §25 tenu par construction** — `resolveEffectiveCapabilities` calcule une **intersection**, jamais une union, donc aucun chemin de code ne permet à une spec d'élargir les permissions ; les refus sont retournés et audités. Rules injectées avec provenance (specId + version), design tokens à noms et valeurs contraints, revues rendues en `ArtifactInput`. **Hors périmètre, écrit dans le module** : génération de code, aller-retour de diagrammes, et specs YAML (ajouter un parseur = décision supply-chain). |
| 12 | Artifact Studio | `PARTIEL` | **Lignage de versions livré** (`47b1b84`, `ArtifactStore` 27/27) : l'id nomme une lignée indépendante du contenu, chaque révision a son manifeste, `history`/`latest`, révisions antérieures préservées, ré-écriture idempotente, et deux lignées au contenu identique coexistent (le bug de collision est corrigé). Export vers outbox avec politique de métadonnées `strip` par défaut. **Complété** (`e5dbfe8`, `ArtifactStudio` 33/33) : `@unifia/artifact-studio` livre le stripping des métadonnées de format (parts `docProps`/`customXml` **et** les références vers elles, pour que le paquet reste cohérent), la preview inerte (refuse macro, relation externe, PDF actif) et le diff sémantique par unités de contenu. **Réserve** : le stripping PDF **refuse** au lieu de prétendre — notre générateur n'émet pas d'`/Info`, donc un PDF qui en a un ne vient pas d'ici et l'éditer demande un vrai parseur. Reste `NON FAIT` : EXIF d'images embarquées. |
| 13 | Memory et session intelligence | `PASS local` | `MemoryRuntime` 4/4 et **`@unifia/memory-governance` 34/34** : provenance typée (user/agent/import/remote), **consentement par référence de source** avant promotion d'un import, déduplication par empreinte normalisée, classification de sensibilité, séparation stricte code/work/personal, et **la règle du §27 — aucune mémoire d'un transport distant sans validation** — imposée comme porte fermée par défaut. Les records classés `secret` sont exclus du contexte de prompt **dans la couche**, pas au point d'appel. |
| 14 | Workflow automation | `PASS local` | `WorkflowRuntime` + `FileWorkflowStore`, et **`@unifia/workflow-catalog` 66/66** : les **8 workflows du §28** livrés, avec les **9 déclarations par étape rendues obligatoires** (un test supprime chacune des neuf et vérifie le refus). Règles inter-champs imposées : une étape irréversible doit être approuvée et ne peut pas déclarer de retry (un retry ré-applique ce qui ne s'annule pas) ; les capabilities critiques du §15 exigent une approbation ; `terminal.run`/`network.request`/`browser.navigate` exigent un sandbox. |
| 15 | Skill Hub et Marketplace | `PASS local` | `SkillHubRegistry` 8/8, manifeste strict, signatures trustées, refus de downgrade, sorties immuables ; routes serveur avec scope. Marketplace distante : `NON FAIT` (et hors périmètre local). |
| 16 | MCP UI Control et Generative UI | `PARTIEL` | Renderer allowlisté 3/3, `McpUiControlBroker` 4/4, routes `/v1/ui/actions` et `/v1/ui/render`. **Transports MCP** : `@unifia/mcp-transport` 32/32. **Consommateur DOM** : `@unifia/generative-ui-dom` 29/29, montage sûr, payloads hostiles couverts. **E2E navigateur réel : `PASS local`** — `GenerativeUiBrowserE2E` 10/10 dans Chromium réel, chaîne render → clic → broker → audit, un clic généré devient une approbation en attente. **`@unifia/mcp-ui-actions` 84/84** livre les **11 actions V1 et 7 actions critiques du §30** : une action critique exige **simultanément** capability dédiée, approbation JIT, audit et confirmation visible. Invariant central : **une UI générative ne peut jamais atteindre une action critique**, `trusted` ou non — « trusted » pour un renderer signifie que son markup est allowlisté, ce qui ne dit rien du modèle qui a produit son contenu. Le refus est évalué **avant** la confirmation, pour ne pas montrer à l'utilisateur une boîte de dialogue pour une action jamais permise. L'allowlist générative est exactement l'ensemble V1, donc une action critique ne peut même pas être **nommée** dans du markup généré. |
| **Gate C** | Plateforme extensible stabilisée | `NO-GO` | Voir `GATE-C-STATUS-2026-08-03.md`. Bloquants : Phase 11 absente, cœur Phase 12 absent, pas de consommateur DOM, pas de bootstrap serveur, audit externe absent. |
| 17 | Release hardening | `PASS local` | Gate de conformance reproductible (8/8) et **`@unifia/release-hardening` : les 36 scénarios nommés du §32 comptabilisés** — 10 exécutés ici (hashes, inventaire binaire, manifeste de mise à jour malveillant, évasion symlink/junction, macros Office, crash orchestrateur, crash runtime, crash worker document, interruption réseau, changement de workspace pendant exécution), 24 couverts ailleurs **avec pointeur vers la suite**, 2 bloqués externes (signatures détachées, protocole N−1 qui n'existe pas encore). La matrice **échoue si un scénario n'a ni test ni couverture**, et refuse tout scénario absent du plan ou inventé : c'est ce qui empêche la liste de se dégrader en case cochée. |
| 18 | Release publique | `BLOQUÉ EXTERNE` | Installers signés, audit externe ciblé computer-use/remote : nécessitent des clés et un auditeur tiers. |

## Ce qui n'est prouvé par aucun test de package

Ces points sont volontairement listés parce qu'une suite verte ne les couvre pas :

1. ~~Aucun processus n'expose le WorkbenchServer.~~ **Clos le 2026-08-04** par `5590c9d`. Le passage aux preuves HTTP réelles a immédiatement révélé trois défauts de production qu'aucun test en mémoire ne pouvait voir : chemin d'erreur mort sur les 17 routes (`return` sans `await` dans `fetch`), contenu de fichier sérialisé en `{"type":"Buffer",...}`, et flux SSE tué par l'idle timeout faute d'octet initial.
2. ~~Aucun consommateur DOM du Generative UI.~~ **Clos le 2026-08-04** par `2783f35` (29/29) puis `637f786` : la preuve en navigateur réel tourne (`GenerativeUiBrowserE2E` 10/10). Chromium réel charge la page sur un vrai socket, monte l'UI décrite par le serveur avec le vrai consommateur, et un clic réel repart en HTTP dans `WorkbenchServer.fetch` jusqu'à l'audit durable — **un clic depuis une UI générée ne s'exécute pas, il devient une approbation en attente**.
3. **Aucun fournisseur MCP externe branché.** Le transport est prouvé contre une paire loopback et des flux injectés, pas contre un serveur MCP réel.
3bis. **Aucun backend OpenCode réel dans la conformance suite.** Les adaptateurs `opencode` et `unifia` y délèguent vers un backend bâti sur `FakeRuntimeAdapter` : c'est une preuve de contrat et de délégation, pas une preuve du runtime OpenCode. **Raison précise, vérifiée le 2026-08-04** : `SessionPrompt.prompt` atteint un modèle, donc le scénario `send-prompt` n'est pas exécutable hors ligne. `createSession`/`listSessions` le seraient, mais une conformance partielle serait plus trompeuse qu'utile. **Corollaire trouvé en enquêtant** : le backend réel ne tenait pas le contrat de rejouabilité (aucun `sequence` émis, `afterSequence` ignoré) — corrigé par `cc6b325`, avec la logique de séquencement extraite dans `SessionEventHub` pour être prouvable sans démarrer de runtime (25/25).
3ter. **Divergence de contrat assumée.** Le plan §7.1 place `replyApproval` sur `RuntimeAdapter` ; l'interface implémentée ne l'a pas. L'ajouter contredirait le plan §5, qui fait de l'ApprovalBroker l'unique autorité des approbations. La divergence est documentée dans la suite plutôt que satisfaite mécaniquement.
4. **Aucune preuve externe** : pas d'audit tiers, pas de pentest, pas de démo 90 minutes, pas de release signée.

## Prochaine action unique

Voir `NEXT-CARD-2026-08-04.md`.
