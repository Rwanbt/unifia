### DECISIONS
- Keep Work mounted in SessionRoute's shared main slot and make WorkSurface own the maquette contract `mode-main > surface-card` | preserves shared chat and independent inspector geometry | separate Work route surface | active
- Reuse real Workbench data and controls while correcting only the shell boundary and spacing in this pass | avoids fabricated task/agent data | rebuilding the Work domain model | active
- 2026-09-23: prioritize the user's requested Editor-panel parity across Code, Work, Design, Automate, Browser and Memory; defer the pasted SearXNG installation and web-voice follow-up | current explicit objective is the v110 portage | continue the adjacent backend/voice thread first | active
- 2026-09-23: do not change panel CSS until fresh app/reference captures at the same 1440x900 viewport identify a concrete mismatch | exact visual parity needs a paired render | infer a correction from source alone | active
- Use the user's requested branch `voice` from `new-ui`, overriding the attached plan's `dev` / `feat/voice-runtime-v2` baseline | user's explicit branch instruction controls this run | follow the attached baseline unchanged | active
- User explicitly authorized implementation without a GitHub Issue and confirmed none exists | direct user instruction on 2026-09-23 | stop at repository claim policy | active
- 2026-09-24: keep Wave H open until the LiveKit agent yields a final transcript and its identity/reconnect/shutdown gates are evidenced; the physical Microphone Array is present, so Wave G is no longer blocked by hardware discovery but still requires the full dictation and UI journey | physical capture plus Parakeet inference now exist; neither proves insertion/no-submit or the other Wave G regressions | declare G/H complete from model inference and unit tests alone | active

### UNCERTAINTIES
- Live visual capture may remain blocked by the existing `127.0.0.1:4101` fetch failure | browser previously showed backend connection errors | retry browser after typecheck and inspect screenshot | P1
- `/browse` is not runnable in this session: the current skill tree has no built `dist/browse`, and the installed Linux binary launch through WSL returned `E_ACCESSDENIED` | superseded by this session's successful Windows Browse `goto`/`click` under explicit escalation | retain historical evidence; use the available Windows Browse executable for local runtime QA | REJECTED (browser launch succeeded)
- Current availability of Vite on `127.0.0.1:4450` and Workbench on `127.0.0.1:4101` is unverified in this turn | Windows listener inspection returned Access Denied | once `/browse` is available, navigate to the app route and inspect requests/bridge state | P1
- R7 is still marked PROPOSÉ and requires explicit O1–O3 decisions before implementation; its expected ADR-042 conflicts with the current untracked ADR-042 title-menu decision, and issue #116 could not be queried | no canonical approval/claim can be confirmed | restore GitHub API access, reconcile issue/ADR authority, then resolve the R7 gate | P1
- No matching open GitHub Issue or active implementation PR was returned for voice/speech/audio/TTS; user confirmed no Issue exists and directed implementation to proceed | GitHub queries returned no matches; user message | do not create an Issue; record policy exception and continue on `voice` | REJECTED (user override)
- LiveKit agent final transcription, reconnect/no-duplicate behavior, and test-process shutdown are not yet confirmed | browser joined and published the synthetic track, then timed out without transcript; worker RTC timed out | diagnose native ICE/RTC connection, then re-run final-transcript, reconnect, duplicate-turn, and shutdown gates | P1
- Native LiveKit RTC on this Windows host still fails to establish the agent media connection despite a working browser publisher; the current server/client pair has a measured ICE topology mismatch (server offers loopback only; Python gathers no loopback candidate) | correlated LiveKit debug logs showed four failed candidate pairs and zero responses; signaling connected successfully | test a supported relay-only route or a client transport that can reach loopback while preserving the plan's localhost-only default | P1
- Physical microphone availability is no longer uncertain: Windows exposes `Microphone Array (Intel Smart Sound Technology for Digital Microphones)` as MME input device 1 with four channels at 44.1 kHz. A 10-second capture produced 441,000 frames (downmix RMS 0.043381, peak 0.447632), and local Parakeet int8 CPU inference returned nonempty French text. Exact user intent was not confirmed; this does not establish PromptInput insertion, no-auto-submit, permission UX, capture ownership, or prompt regression gates | voice-host Python environment, PortAudio device enumeration, 10-second sounddevice capture and Parakeet inference; prior no-device inventory is superseded | old conclusion `Physical microphone qualification remains unavailable` is REJECTED; physical capture/model inference confirmed, full Wave G remains open

### VERIFIED FINDINGS
- Checkout at reprise: `D:\App\unifia\_a7-automate-memory`, `new-ui`, HEAD `e9ecacd32cad53eed2180b870ffe7a1f50263c11`, six commits ahead and 69 dirty/untracked status entries | `git rev-parse HEAD; git status --short --branch` -> exact SHA, `[ahead 6]`, 69 entries | confirmed
- The reference contains six target views (`view-code`, `view-work`, `view-design`, `view-automate`, `view-browser`, `view-memory`); each app destination emits `data-v110="surface-card"`, while Code uses `SessionEditorSurface` | reference HTML:15363,15423,15474,15663,15695,15778; `packages/app/src/pages/session/session-editor-surface.tsx:196-197`; workbench surface files | direct source read | confirmed
- Claude's supplied transcript reports SearXNG implementation and 402 tests/typecheck passing; the code/ADR/test files are present as uncommitted work, but those checks were not rerun in this reprise | transcript supplied by user; `git status --short`; `packages/unifia/src/tool/searxng.ts`, `packages/unifia/test/tool/searxng.test.ts`, `docs/adr/ADR-044-web-search-through-self-hosted-searxng.md` | presence read directly; validation not independently verified | confirmed
- Vault daily sync failed and did not mark the day: `PermissionError` on `D:\Documents\Obsidian\IA_Dev_Brain\.git\vault-sync.lock` | `pwsh -NoProfile -File 'D:/scripts/vault_sync_once_daily.ps1'` -> vault sync failed, day NOT marked | confirmed
- Current R7 says PROPOSÉ and requires O1–O3 to be recorded in issue #116 and ADR-042 before implementation; the current untracked `docs/adr/ADR-042-session-menu-in-chat-head.md` records a different decision | `docs/ui-reference/v110/PLAN-PIXEL-PERFECT-PORT-2026-09-17-R7.md:5-6,59-63,493`; ADR file read directly | confirmed
- GitHub issue #116 status could not be verified: `gh issue view 116 --repo Rwanbt/unifia --json title,state,assignees,url` failed because the configured proxy at `127.0.0.1:9` refused the connection | command output | `GITHUB_STATE_UNAVAILABLE` | confirmed
- Current Voice checkout is `D:\App\unifia\voice-runtime`, branch `voice`, HEAD `9c083a2b6a4f7f9443680d264f2168809d41c7f7`; status shows 34 dirty entries (18 tracked, 16 untracked) including this updated state file | `git rev-parse --show-toplevel; git status --short --branch; git rev-parse HEAD` -> exact checkout, `## voice...origin/voice`, exact SHA; `git status --porcelain=v1` -> 34 lines | confirmed
- Wave H local validations from 2026-09-24: app typecheck, Bun transport 6/6, Voice Host 36/36, targeted Ruff, Rust `cargo check --lib --locked`, rustfmt and diff-check passed; Rust test executable failed before harness with `0xc0000139` and is not a pass | command outputs in this session and `projects/unifia/operations/sessions/session-voice-wave-h-local-parakeet-runtime-2026-09-24.md` | confirmed
- Actual local model execution: Silero ONNX emitted speech start/end; local `v1-mini` EOT probability was 0.82320; Parakeet int8 CPU returned a nonempty transcript for the synthetic 24 kHz fixture (37 chars, SHA-256 `417de33aea09eb7d958f5adcd354b3a6f5c21c2b9d5afaebac1b2a530194af9d`) | runtime outputs summarized in the linked session note | confirmed
- Requested local vault save completed: session note plus appended Unifia memory and gstack checkpoint exist; daily sync failed on `.git/vault-sync.lock`, so no remote sync/commit/push is claimed | `Test-Path`, `Get-Content -Tail 14`, hashes `CE692F79C0A6B89458CD4A6BD9BBAE3FD87C2E829311B46C9BAA6D9F668B4423` and `AB0E4E221F6A653059068D9051B88EF4EA354FC77A038C1C42481202AC5C8350`; sync command output | confirmed
- Actual browser harness connected to LiveKit room `voice-parakeet-20260924-1049` and published its synthetic microphone track, then reported `LIVEKIT_PARakeet_STT_TIMEOUT`; no transcript JSONL was created | Browse `goto` -> HTTP 200; `click #start`; `browse text` -> `connected-and-published identity=browser-publisher`, then `LIVEKIT_PARakeet_STT_TIMEOUT`; `Test-Path` result file -> absent | confirmed
- Worker job on PID 22820 failed RTC connection twice with `wait_pc_connection timed out`; an isolated LiveKit 1.13.7 debug instance bound only to `127.0.0.1:17890` and UDP `127.0.0.1:17892`, but the replacement worker never registered during the observation window | `.build-temp/livekit-agent-runtime.stdout.log` latest events at `2026-09-24T08:55:18Z`; `netstat -ano` for 17890/17892; worker debug stdout/stderr empty | confirmed
- Fresh native Python RTC probe on 2026-09-24: LiveKit 1.13.7 on loopback recorded four `processing join request` events with client SDK `PYTHON`/Windows and an SDP offer, but the RTC client still ended at `wait_pc_connection timed out`; the no-proxy PCM smoke emitted the same ICE/peer-connection timeout | `.build-temp/livekit-rtc-ice-detail.log` (event count performed without printing SDP); Python probe output; same-host port 17880 listener/UDP 17882; process stopped after probe | confirmed
- The shell inherited `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY` and `NO_PROXY`; a probe with those variables removed no longer reported only `os error 10061` and advanced to the ICE wait timeout. Desktop worker launch already removes upper/lower HTTP(S)/ALL proxy variables and sets loopback `NO_PROXY`, so the proxy environment changes direct-probe symptoms but does not explain the app worker's remaining ICE failure | process environment names only (values withheld); `livekit_runtime.rs:374-385`; native client output | confirmed
- Paired same-instance transport check on 2026-09-24: Chromium publisher/receiver both connected to the same loopback LiveKit 1.13.7 process, selected ICE and reported `WEBRTC_MEDIA_PASS`, 185612 outbound and inbound RTP bytes and 751 received audio packets. A direct Python RTC 1.1.18 client to that same server with HTTP(S)/ALL proxies removed still failed `wait_pc_connection timed out`; its signaling TCP connection was established. This isolates the remaining failure to the native Python RTC/ICE path on this Windows host, not the server's loopback bind or missing signaling reachability; exact candidate/ICE cause remains unknown | Browse smoke page result and Chromium console; `livekit_transport_smoke_loopback.py` direct client output; server listener PID 13140, client-side established sockets, loopback UDP 17882; server and HTTP processes stopped afterward | confirmed
- Test processes and listeners were stopped after the probes; no listeners remained on TCP 17880/17990/17890 or UDP 17882/17892 | owned-PID Stop-Process; subsequent `netstat -ano -p tcp/-p udp` filtered to those ports returned no rows | confirmed
- Local session note and memory pointer saved; no code, `LOG.md`, vault commit or push changed/verified | direct read of `projects/unifia/operations/sessions/session-ui-v110-editor-parity-resumption-2026-09-23.md` and top of `projects/unifia/_memory/memory.md` | confirmed
- WorkSurface was mounted from the session mode switch but rendered a full padded section without `mode-main > surface-card` | packages/app/src/pages/session.tsx:1104-1115 and packages/app/src/pages/workbench/work-surface.tsx:141-234 | source read directly | confirmed
- The reference Work mode uses `mode-main > surface-card > work-view` | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:15423-15470 | source read directly | confirmed
- After restarting only the new-ui Vite service on port 4450, the live Work route rendered the shared chat plus Work content (Work heading, view tabs, Progression, Prochaine action sûre, operation cards) inside the main surface | browser URL `/work?session=audit-check`, screenshot captured 2026-09-22 | CUA live screenshot and accessibility tree | confirmed
- The Workbench bridge remains unavailable, so live values are honest empty-state values rather than fabricated tasks or runs | browser accessibility tree text "Le pont Workbench est indisponible" and 0 tasks/runs | live browser observation | confirmed
- Work previously hardcoded non-Code modes to `split` and mounted their surface regardless of the Chat/Split/Editor state | packages/app/src/pages/session.tsx:196-208 and :1104-1115 | source read directly | confirmed
- Code and Work were compared live at the same responsive viewport: both keep chat above the main surface in Split/Editor and remove the main surface in Chat | CUA screenshots and accessibility trees on Code and Work routes | confirmed
- Work now derives geometry from `view().workspace.current()` for every session mode, and its surface is gated by `workspace !== "chat"` | packages/app/src/pages/session.tsx | source read directly and targeted test | confirmed
- Work now shares the desktop `height: calc(100% - 24px)` and `margin-block: 12px` gutter rule with Code through `[data-component="workbench-mode-main"]` | packages/app/src/styles/v110-editor.css | source read directly and targeted test | confirmed
- DesignSurface now owns the same `mode-main > surface-card` shell as Code and Work while preserving the existing DesignWorkspace/Canvas content | packages/app/src/pages/workbench/design-surface.tsx | source read directly, typecheck, targeted test, and live screenshot | confirmed
- Live Design verification: Chat hides Design, Split and Editor render the shared chat plus DesignWorkspace in the main panel; the switch values are independently confirmed in the accessibility tree | browser URL `/design?session=audit-check` | CUA screenshots and accessibility tree | confirmed
- Previous Settings implementation was incorrect: it treated Settings as a `?settings=1` query overlay and left the rail contract as a dialog action | superseded by the reference audit | source read directly | superseded
- Settings now behaves as a switchable workspace destination: the rail action navigates to `/<workspace>/settings?session=...`, the destination is active/pressed, the session is preserved, and the editor slot mounts `SettingsSurface` from the route | packages/app/src/context/mode-directory.ts, packages/app/src/context/mode.tsx, packages/app/src/pages/layout.tsx, packages/app/src/pages/session.tsx | 55 targeted tests, typecheck, diff check, live click/return verification | confirmed
- Reference contract for Settings: `#view-settings > .mode-shell > .mode-main > .surface-card.settings-surface`; integrated navigation is a workspace surface, not a modal | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:16090-16170, 2250-2378 | direct reference read | confirmed
- Fixed exclusive selection regression: the rail no longer compares shell `active()` plus an independent Settings boolean; desktop and mobile now consume one reactive `activeDestination`, so Code is unselected whenever Settings is active | packages/app/src/pages/layout/sidebar-shell.tsx, packages/app/src/shell/v110-mobile-nav.tsx, packages/app/src/pages/layout.tsx | typecheck, targeted tests, live AX state and screenshot | confirmed
- Account Center now follows the reference's `#view-user` contract as a real workspace destination: avatar navigation reaches `/<workspace>/user?session=...`, the rail selection is exclusive, the breadcrumb changes to Compte, and the editor slot renders an integrated account surface with overview, personal, organisations and security pages | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:15936-16075, 5435-5490, 11234-11257; packages/app/src/context/mode-directory.ts, packages/app/src/pages/settings/user-surface.tsx | 42 targeted tests, typecheck, diff check, live avatar click and screenshot | confirmed
- Browser and Memory are now first-class workspace destinations instead of aliases: dedicated `/browser` and `/memory` routes preserve the session, their rail entries have exclusive active state, and their editor surfaces wrap the existing real Browser and Memory panels; Automate continues using the existing real AutomateSurface | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:15663-15790; packages/app/src/context/mode-directory.ts, packages/app/src/pages/workbench/browser-surface.tsx, packages/app/src/pages/workbench/memory-surface.tsx, packages/app/src/pages/session.tsx | 64 targeted tests, typecheck, diff check, live Browser/Memory screenshots | confirmed
- Automate invalid-mode regression fixed: the route parser no longer rejects `/automate` when `workflow.run` is absent, so the existing real `AutomateSurface` mounts and reports the unavailable desktop bridge/capability state; workflow execution remains enforced at the Workbench/server boundary | packages/app/src/context/mode-directory.ts, packages/app/src/context/mode.tsx, packages/app/src/pages/workbench/connection-banner.tsx, packages/app/src/pages/workbench/automate-surface.tsx | 79 targeted tests, typecheck, diff check, live rail switch and screenshot; Automate active with `Disponible dans l'application desktop` and bridge-unavailable alert | confirmed
- Automate now uses the same editor surface contract as Code, Work and Design: `mode-main > surface-card > AutomateSurface`, with the shared desktop gutters and switch-controlled geometry | packages/app/src/pages/workbench/automate-surface.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | 34 targeted tests, typecheck, diff check, live bounding boxes in Split/Editor and Chat unmount verification; Split 579px wide, Editor 927px wide, Chat 0 Automate surfaces | confirmed
- Chat composer parity corrected: the normal prompt no longer appends a rotating example, and the copy-context action is docked at the lower-right conversation edge above the composer instead of occupying the conversation header | packages/app/src/components/prompt-input/placeholder.ts, packages/app/src/pages/session.tsx, packages/app/src/styles/v110-chat.css, packages/app/src/pages/session/session-workspace-layout.test.ts | 24 targeted tests, typecheck, diff check, live AX placeholder `Demandez n'importe quoi...`, and live button bounds bottom-offset 112px | confirmed
- Direct deep-link sidebar regression fixed: Layout now registers the active route directory in the browser-local project registry when it is missing, allowing `context-panel` to render Projects, active workspace and session content outside the Codex harness | packages/app/src/pages/layout.tsx | typecheck, targeted layout/session tests, diff check, and live Brave AX verification: `PROJETS 1`, active `_a7-automate-memory`, `PORTÉE DU CODE 1`, `Nouvelle session` | confirmed
- Context panel mode sections now dispatch from `mode.destination()`: Code keeps live `Code Scope`; Work renders `Vue` and `Agents`; Design renders `Fichiers` and `Assets`; Automate renders `Workflows` and `Runs`; Browser renders `Project Links` and `Library`; Memory renders `Navigation` and `Raccourcis` | packages/app/src/pages/layout/sidebar-panel-mode-sections.tsx and sidebar-panel-mode-sections.test.ts | targeted test, typecheck, diff check, and live Brave verification across all six mode switches; Work screenshot shows both groups in the open floating context panel | confirmed
- InspectorFrame now keeps one shared frame and three shared tabs while dispatching Explorer, Inspector and Execution content by `mode.destination()`: Code keeps the live FileTree and its Inspector is limited to session metadata; legacy theme sliders are excluded. Work/Design/Automate/Browser/Memory (plus Settings/User) receive maquette-matched mode content and cards | packages/app/src/pages/session/session-side-panel.tsx, packages/app/src/pages/session/mode-inspector-content.tsx, packages/app/src/pages/session/mode-inspector-content.test.ts | targeted tests, typecheck, diff check, and Brave live verification | confirmed
- Topbar right actions now match the maquette: the former file-tree toggle is the context-to-Work-task action and the former copy-path fallback is Open in; copy-path was removed from the Open in menu. | packages/app/src/components/session/session-header.tsx, packages/ui/src/components/icon.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | 21 layout tests, typecheck, diff check, and Brave screenshot/ARIA verification | confirmed
- Topbar chrome colors now use the canonical v110 palette: translucent `#121214` background, `#2a2a2f` hover, `#1b1b1e` active, and `rgba(255,255,255,.07)` shell line. | packages/app/src/styles/v110.css, packages/app/src/pages/session/session-workspace-layout.test.ts | 22 layout tests, typecheck, diff check, and Brave screenshot verification | confirmed
- Inspector audit corrected the structural gap: the shared frame now has the maquette `inspector-head` title/close row, a dedicated three-tab grid (`Explorer`, `Inspecteur`, `Exécution`) and one flex content stage; mode cards now expose the maquette card/row contracts and neutral v110 surfaces instead of brand-blue card tokens. | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:16980-17000, 9458-9465, 10301-10312; packages/app/src/shell/v110-inspector-frame.tsx, packages/app/src/styles/v110-inspector.css, packages/app/src/pages/session/mode-inspector-content.tsx | targeted tests 26 pass / 144 assertions, typecheck, diff check, Brave live verification across Code/Work/Design/Automate/Browser/Memory with final Design screenshot | confirmed
- Inspector parity follow-up fixed the missed nested controls: Code now exposes the reference sub-navigation `Overview`, `Symbols`, `Search`, `Review`, `Git`, `Context`, `History` (the reference source contains seven visible labels, including Overview), and Exécution now exposes all 11 reference filters `Tout`, `Modèle`, `Contexte`, `Outils`, `Sources`, `Skills`, `Mémoire`, `Agents`, `Règles`, `Interaction`, `Usage`, with clickable active state. | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:23783-23784, 26883-26903; packages/app/src/pages/session/mode-inspector-content.tsx, packages/app/src/styles/v110-inspector.css | targeted tests 27 pass / 156 assertions, typecheck, diff check, Brave live count Code=7 and Exécution=11 plus screenshots | confirmed
- Inspector organization audit corrected the visual composition: Code sub-tabs and Execution filters now use the reference's single horizontal, non-wrapping rows with exact compact dimensions, spacing, borders and overflow behavior; the nested Code scroller was removed so no extra scrollbar appears. | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:10301-10312, 12452-12454; packages/app/src/pages/session/mode-inspector-content.tsx, packages/app/src/styles/v110-inspector.css | targeted tests 27 pass / 160 assertions, typecheck, diff check, Brave screenshots for Inspector and Execution with Code=7 and Exécution=11 | confirmed
- Hover interaction regression fixed: the rail, context panel and inspector now use the maquette's pointer-only trigger/panel state machine; duplicate mouse enter/leave handlers were removed so leaving the trigger no longer races the panel's enter event and closes the preview before the pointer reaches it. | docs/ui-reference/v110/Unifia-UI-UX-v110-PORT-READY-R1.html:25835-25907; packages/app/src/context/layout.tsx, packages/app/src/components/titlebar.tsx, packages/app/src/pages/layout/sidebar-shell.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | regression contract 27 pass / 164 assertions, typecheck, diff check, no duplicate hover handlers, Brave live screenshot after HMR | confirmed
- Hover handoff audit completed: preview surfaces now become immediately capturable without width animation, receive pointer movement explicitly, and use a geometry-based trigger-to-panel corridor; the context panel and inspector both stayed open during the real coordinate transfer and closed after leaving the corridor. | packages/app/src/context/layout.tsx, packages/app/src/pages/layout/sidebar-shell.tsx, packages/app/src/pages/session/session-side-panel.tsx | 22 targeted tests pass / 123 assertions, typecheck, diff check, Brave live context-panel and inspector corridor screenshots plus outside-close checks | confirmed
- Inspector fermé ne laisse plus de barre verticale : l'état caché neutralise la marge, la bordure et la largeur effective du card shell, tandis que le wrapper interne reste en `box-border`. | packages/app/src/styles/v110-inspector.css, packages/app/src/pages/session/session-side-panel.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | 22 tests / 126 assertions, typecheck, diff check, Brave live computed `outerWidth=0`, `borderLeft=0px`, `margin=0px` et screenshot | confirmed
- Navigation latérale au survol ne réserve plus la largeur de la barre repliée : le décalage central utilise la largeur du seul panneau si le rail n’est pas ouvert, et la largeur combinée uniquement quand rail + panneau sont visibles. | packages/app/src/pages/layout.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | 22 tests / 128 assertions, typecheck, diff check, Brave live: rail `0px`, panneau `246px`, workspace décalé à `276px` (bord aligné au panneau), état initial restauré après contrôle | confirmed
- Gutter latéral aligné sur les 12px verticaux de la maquette : rail à 12px du bord gauche; en Inspector fermé, Editor à 12px du bord droit; avec Inspector ouvert, les deux cards gardent 12px de respiration et 12px entre elles. | packages/app/src/styles/v110.css, packages/app/src/styles/v110-editor.css, packages/app/src/pages/session.tsx, packages/app/src/pages/session/session-workspace-layout.test.ts | 22 tests / 134 assertions, typecheck, diff check, Brave live: rail x=13px et bas=666/679, Editor right=1523/1536, Inspector open gap=12px et right=12px; interface restaurée Inspector fermé | confirmed
- Context panel aligné au bord gauche quand le rail est rétracté : le rail caché ne conserve plus sa marge ni sa bordure, et la carte démarre à 13px du bord extérieur. | packages/app/src/pages/layout/sidebar-shell.tsx, packages/app/src/styles/v110.css, packages/app/src/pages/session/session-workspace-layout.test.ts | 22 tests / 137 assertions, typecheck, diff check, Brave live: context x=13px, rail width=0/margin=0/border=0, screenshot ouvert | confirmed
- `new-ui` was committed and pushed at `6ff80084c6e733c2fed199e9450a8ad0fd4919de`; `origin/new-ui` equals local HEAD and divergence is `0 0` | `git rev-parse HEAD; git rev-parse origin/new-ui; git rev-list --left-right --count origin/new-ui...new-ui` -> same SHA, `0 0` | confirmed
- `voice` worktree is at the same commit after `git merge --ff-only new-ui`; this checkpoint's `state.md` is its only local modification and no product code is changed | `git status --short --branch; git rev-parse HEAD; git diff --stat` -> `## voice`, `6ff80084c6e733c2fed199e9450a8ad0fd4919de`, `state.md | 7 +++++++` | confirmed
- Voice Wave A characterization and initial Wave B contracts/settings migration committed as `7d8dd95a80`; added stable speech/provider contracts, strict voice registry lookup, v2 settings migration, tests, ADR draft, and baseline record | targeted tests: 8 app tests/18 assertions and 4 contract tests/9 assertions; contracts typecheck and isolated settings typecheck passed | confirmed
- Wave A performance baseline captured on Windows for Pocket solo in EN/FR/ES/IT/DE and one simultaneous French Pocket + Bonsai 2 27B GPU run; Gate A passes as exploratory characterization, with repeat variance and missing STT/Live metrics explicitly listed | `docs/voice-runtime-baseline.md`; Pocket CLI output and local LLM timings | confirmed
- Speech language router committed as `b94b212a2f`; automatic short utterances preserve the prior language, while explicit setting and longer final STT take precedence | 6 contract tests/16 assertions and contract typecheck passed | confirmed
- Wave B now uses the explicit `unifia-audio-settings.v2` key, reads the legacy key as migration input, and writes normalized v2 values; current settings UI switches in Wave C alongside Kokoro removal | 10 app tests/24 assertions, 5 settings tests/16 assertions, 6 contract tests/17 assertions, isolated settings typecheck and contract typecheck passed | active
- New-ui pending changes were committed after `bun typecheck`, 3 targeted tests/22 assertions, and `git diff --check`; `.build-temp/` stayed untracked | command outputs and commit `6ff80084c6` | confirmed
- GitHub issue and PR queries ran successfully but produced no matching open voice/speech/audio/TTS Issue or relevant PR | `gh issue list ...` and `gh pr list ...` -> exit 0, empty match set | confirmed
- IPv6 LiveKit probe on 2026-09-24: Python 3.12 SDK completed the WebSocket handshake to `ws://[::1]:17880`, then failed all three RTC attempts with `wait_pc_connection timed out`; during PID 21612 the UDP sockets were `172.27.240.1:56263`, `192.168.48.1:56264`, and `192.168.1.25:56265`, with no loopback UDP socket observed. IPv6 signaling therefore does not resolve the native ICE failure. The owned LiveKit/static HTTP test processes were stopped and ports 17880/17882/17990 no longer listen | `.build-temp/livekit-python-ice-ipv6-probe.log`; `netstat -ano -p UDP` while PID 21612 active; post-stop `Get-Process`/`netstat -ano` | confirmed
- LiveKit 1.13.7 debug server correlated the native Python 1.1.18 offer with its actual ICE candidates: server local candidate was loopback `127.0.0.1:17882`; all 11 client remote candidates were non-loopback (10 host + 1 server-reflexive); the four resulting candidate pairs all failed with zero responses. This confirms an ICE candidate topology mismatch for the current client/server pair. The client also gathered a server-reflexive public address, revealing default STUN egress that must be removed or explicitly kept local to satisfy the no-automatic-Internet rule. Production server config already binds both signaling addresses to loopback, enables loopback candidate collection, and restricts server RTC IPs to `127.0.0.0/8` and `::1/128` | `.build-temp/livekit-python-ice-debug-server.log` (IP/ICE credentials redacted after analysis); `livekit_server.rs:277-278`; package `livekit/rtc/room.py:96-107`; test results parsed from server candidate/pair logs | confirmed
- TURN relay-only probe on 2026-09-24 is INVALID/INCONCLUSIVE: the probe script hardcodes `.build-temp/livekit-loopback-turn-test.yaml` and `ws://[::1]:17880`, while the running server used `.build-temp/livekit-loopback-turn-advertised-test.yaml`; therefore it did not exercise the advertised TURN config. Client still timed out; do not count this as a TURN compatibility result | script lines in `livekit-python-ice-relay-probe.py`, server config path, client log; server saw no ICE pair stats; after Ctrl-C no UDP listeners remained on 17882-17886 | confirmed
- The pinned desktop LiveKit config now provides an explicit loopback-only STUN URL (`127.0.0.1:7882`) so the server/client config will not inherit public STUN defaults; a separate LiveKit 1.13.7 smoke instance accepted the matching config and listened on its configured loopback HTTP port, with no public STUN host in its startup logs | `packages/desktop/src-tauri/src/livekit_server.rs:275-327`; `.build-temp/livekit-loopback-no-egress-test.yaml` and startup log; official LiveKit config sample documents Google's STUN defaults | source/test config inspected; standalone config parse+loopback bind confirmed
- Targeted app voice tests after physical microphone discovery: web dictation, coordinator ownership, and LiveKit transport = 19/19 tests, 569 assertions; Rust config unit test compiled but its Windows test executable exited before the harness with `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`, so no Rust test pass is claimed | `bun test src/hooks/web-speech.test.ts src/voice/audio-capture-coordinator.test.ts src/voice/livekit-transport.test.ts`; `cargo test --lib livekit_server::tests::runtime_config_scopes_rtc_to_loopback --locked` | app tests confirmed; Rust harness setup failure confirmed
- Clean embedded-TURN-only probe on 2026-09-24 removed the duplicate `rtc.turn_servers` listener from the prior test, retained loopback signaling/RTC/TURN binds, and started the pinned LiveKit 1.13.7 server. The Python client still returned `ConnectError`; the diagnostic logger captured no client events, so this establishes another failed connection but does not isolate the TURN allocation failure. The owned test server PID 15376 was stopped and ports 18088/18090/18091 were confirmed closed | `.build-temp/livekit-loopback-embedded-turn-only.yaml`, `.build-temp/livekit-embedded-turn-probe.py`, `.build-temp/livekit-embedded-turn.stderr.log`; command output; post-run `Get-Process` and listener checks | confirmed
- Follow-up embedded-TURN probes with client logging and server Pion debug: default RTC and explicit relay transport both returned `ConnectError`. Across four JoinRequests per probe, the server sent loopback host candidates; the default client supplied host candidates on non-loopback interfaces and no relay candidate was observed. The explicit relay probe used a locally generated time-limited TURN credential and also produced no relay candidate. Candidate checks still had no usable pair; no claim of TURN success. Each harness terminated its own server in `finally` | `.build-temp/livekit-embedded-turn-default-{client.raw,server}.log`, `.build-temp/livekit-embedded-turn-explicit-{client.raw,server}.log`; sanitized event/candidate classification | confirmed
- Sanitized re-read of the later UDP/TLS TURN diagnostics confirmed the client received four JoinResponses per run with the embedded TURN URLs (`turn:127.0.0.1:17883`, and for the combined run `turns:turn.localhost:17884`). Across those logs the SDK emitted 44 host candidates and zero server-reflexive, relay, or peer-reflexive candidates; thus TURN advertisement is confirmed, but allocation/candidate gathering is still failing and the precise native RTC cause is unresolved | `.build-temp/livekit-turn-all-debug.client.stdout.raw.log`, `.build-temp/livekit-turn-udp-ipv4.client.stdout.raw.log`, `.build-temp/livekit-turn-udp.client.stdout.raw.log`; local parser output reports only URL schemes/loopback endpoints and candidate-type counts, never credentials | confirmed
- Native TURN send failure is now localized: the Windows WebRTC FFI attempted Allocate from Hyper-V, Docker, and Wi-Fi sockets toward the loopback TURN endpoint; Windows rejected sends with WSAEADDRNOTAVAIL (`10049`). The captured runs contain 72, 144, and 192 such failures respectively and zero relay candidates. The pinned Python `RtcConfiguration` only exposes ICE transport policy, gathering policy, and ICE servers; it has no supported local-interface selection option. The next fix must preserve the plan's localhost-only default instead of binding a LAN address implicitly | `.build-temp/livekit-turn-{all-debug,udp-ipv4,udp}.client.stdout.raw.log`; sanitized code/error counts; installed `livekit/rtc/room.py` and upstream `livekit/python-sdks` `room.py` | confirmed
- Root cause detail for the native TURN gather failure: LiveKit's Windows WebRTC FFI attempted TURN Allocate from its Hyper-V, Docker, and Wi-Fi local sockets toward the loopback TURN endpoint; Windows rejected each send with WSAEADDRNOTAVAIL (`10049`). The three captured runs contain respectively 72, 144, and 192 such failures and zero relay candidates. The pinned Python `RtcConfiguration` API only exposes ICE transport policy, gathering policy, and ICE server list; no supported local-interface selection setting is exposed there. Fixing this without changing the plan's loopback-only default needs a supported same-host transport/interface solution, not a LAN bind by assumption | `.build-temp/livekit-turn-{all-debug,udp-ipv4,udp}.client.stdout.raw.log`; sanitized count of error code and Allocate requests; installed `livekit/rtc/room.py` `RtcConfiguration`; upstream `livekit/python-sdks` `room.py` | confirmed
- `/browse` Chromium launch succeeded only with execution escalation; the app endpoints `http://127.0.0.1:4450`, `http://localhost:4444`, and `http://127.0.0.1:3000` refused connections. Starting a new Vite server was rejected by automatic review because `packages/app/AGENTS.md` says never to launch/restart the app/server. No UI microphone insertion/no-submit evidence was produced in this turn | Browse `status/goto`; package-specific AGENTS.md; rejected `Start-Process bun dev` request | confirmed
- Vault daily sync was retried at session start and failed again with `PermissionError` on `D:\Documents\Obsidian\IA_Dev_Brain\.git\vault-sync.lock`; the day was not marked. No sync, commit, or push is claimed for vault notes | `pwsh -NoProfile -File 'D:/scripts/vault_sync_once_daily.ps1'` output | confirmed

## 2026-09-24 — Voice continuation: explicit LAN scope

- Confirmed active checkout: `D:\App\unifia\voice-runtime`, branch `voice`, HEAD and `origin/voice` both `9c083a2b6a4f7f9443680d264f2168809d41c7f7`; existing implementation remains uncommitted and unpushed.
- Wired saved `voiceHostMode` through the audio settings UI, `LiveKitTransport`, Tauri `live_voice_start`, and ephemeral LiveKit config. Local loopback remains the default; LAN requires an explicit settings selection and binds only a detected private IPv4 address. Hardened interface probing to continue after per-route socket errors. Added translations to all 17 app locales.
- Validation: app unit suite 1,665 passed / 0 failed; `bun run typecheck` passed; `cargo check --lib --locked` passed; `rustfmt --check` on the two changed Rust modules and scoped `git diff --check` passed.
- Rust runtime tests compiled with default features, but the Windows test executable exited before the harness with `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`; no Rust unit test pass is claimed. `--no-default-features` is invalid for this crate because Parakeet's `ort`/`ndarray` dependencies are unconditional in the module tree. Global `cargo fmt --check` also reports ordering diffs in already dirty `lib.rs` and `main.rs`; the changed modules are formatted.
- Wave G remains open for end-to-end PromptInput insertion/edit/no-submit/permission proof; Wave H remains open because the native LiveKit agent has not delivered a final transcript. No app or LiveKit server was launched in this pass. The previously measured Microphone Array capture and local Parakeet inference remain valid hardware/model evidence only.
- Vault daily sync was attempted at session start and failed on `D:\Documents\Obsidian\IA_Dev_Brain\.git\vault-sync.lock` with `PermissionError`; no vault sync, commit, or push is claimed. The version-pinned `livekit==1.1.18` lockfile constraint remains unresolved.
- Additional Rust gate: `cargo clippy --lib --locked -- -D warnings` completed successfully after the checkpoint entry.

## 2026-09-24 — reprise micro physique et sous-gate dictée

- Checkout mesuré : D:\App\unifia\voice-runtime, branche voice, HEAD = origin/voice = debe25deca6c11a977f6ed7122f4aaf5a51df76e ; aucune modification de code dans cette reprise, arbre de campagne préexistant préservé.
- Correction de contexte confirmée par l'utilisateur : le Microphone Array est bien disponible. La preuve matérielle conservée dans le checkpoint Wave G précédent est une capture PortAudio de 10 s (441 000 frames, 4 canaux, 44,1 kHz, downmix RMS 0.043381, peak 0.447632), suivie d'une inférence Parakeet v3 int8 sur CPU qui a produit du texte français non vide. L'audio et le texte ne sont pas conservés.
- Vérifié le contrat applicatif : prompt-input.tsx émet stt-start/stt-stop ; les hooks desktop et mobile insèrent le résultat par execCommand('insertText') et ne soumettent pas le formulaire. Les événements speech-ended gèrent le retour de permission refusée. Le coordinator donne un seul lease de capture et ferme les flux tardifs.
- Vérification actuelle : tests ciblés web-speech, audio-capture-coordinator et prompt-input/submit : 18 pass / 0 fail. Suite packages/app : 1 690 pass / 0 fail.
- Wave G reste ouverte : l'insertion dans le PromptInput réel sous l'application desktop de ce checkout n'a pas été observée. packages/app/AGENTS.md interdit de lancer ou redémarrer l'app/le serveur ; les ports existants disponibles précédemment ne correspondaient pas de façon vérifiable au checkout voice. Ne pas requalifier une simulation comme E2E.
- Wave H reste bloquée par ICE/RTC natif LiveKit sur loopback ; ne pas ouvrir I tant que G et H n'ont pas leurs preuves.
- Synchronisation quotidienne du vault échouée en début de tour sur .git/vault-sync.lock (PermissionError), verrou intact. Cette entrée locale ne signifie ni sync, ni commit, ni push du vault.

### Reprise

1. Garder l'implémentation de capture et les artefacts préexistants intacts.
2. Obtenir une application Voice déjà lancée et attribuable à ce checkout, puis dicter une phrase connue dans un draft non vide, vérifier l'insertion et l'absence de submit, éditer et envoyer manuellement.
3. Résoudre / prouver la topologie loopback RTC avant Wave I.

## 2026-09-24 — investigation de visibilité PortAudio

- Un contrôle strict sur le libellé anglais du Microphone Array a d'abord échoué. L'inventaire complet via sounddevice 0.5.6 expose bien les entrées MME et DirectSound; le libellé MME contient un caractère Unicode remplacé à l'affichage par « � », cause du non-match textuel. Hypothèse vérifiée par l'inventaire, pas un périphérique absent.
- La sélection stable par index MME 1 a ouvert le Microphone Array : 4 canaux, 44,1 kHz, capture mémoire de 132 300 frames / 3 s. RMS downmix 0.000331, pic 0.015915 ; aucune parole connue n'était présente, donc ce n'est pas une qualification STT.
- Aucune correction produit requise : le non-match était dans le script ponctuel de vérification, pas dans l'implémentation Voice. Tests app du tour précédent restent verts; pas de fichier de capture/audio écrit.

## 2026-09-24 — convergence des deux implémentations Live (Waves H–M)

- Le commit parallèle 196186f et la branche Live ont été fusionnés. Conservés : `livekit_server.rs` (binaire officiel 1.13.7 téléchargé et épinglé SHA-256, ports 7880/7882, bind IPv6 conditionnel), `voice_host/live/` (agent, pont, TTS/STT streaming), routes serveur `/voice/live`, UI prompt/settings. Retirés comme doublons : `livekit_runtime.rs`, `livekit_auth.rs`, `voice_live/config.rs`, build Go `livekit-server.ts`/`livekit-server.json`, `live_agent.py`, `parakeet_stt.py`, `livekit-transport.ts`, section « voiceNetwork » des réglages.
- Cause de l'échec ICE `wait_pc_connection timed out` reproduite sous Linux : (1) absence de `rtc.enable_loopback_candidate: true` ; (2) `rtc.stun_servers` pointant sur le port UDP mux de LiveKit lui-même (le mux ne répond pas comme serveur STUN). Sonde isolée : config sans (2) → 50 trames audio reçues ; avec (2) → timeout ; `127.0.0.0/8` vs `/32` sans effet. Correctif : STUN loopback mort `127.0.0.1:9` (aucun STUN public, aucun trafic sortant). Test d'intégration transport réel repassé au vert.
- Hypothèse à vérifier sur Windows : l'échec du client Python RTC alors que Chromium passait (ligne « Paired same-instance transport check ») est compatible avec (2). Parcours physique Windows toujours requis.
